# Pilar vision
Crea public/vision.js, test/vision.test.ts, docs/VISION.md. Módulo ES sin efectos al importar. Exporta frameDifference(previous:Uint8ClampedArray,current:Uint8ClampedArray):number promedio abs RGB normalizado 0..1, alpha ignorada, valida tamaños iguales múltiplo4. Exporta class ChangeGate constructor({threshold?:number,cooldownMs?:number,settleMs?:number,now?:()=>number}), method update(frame:Uint8ClampedArray):boolean, reset():void. Primera imagen devuelve true; después sólo cuando hubo cambio respecto a último frame enviado y el movimiento entre frames recientes se estabiliza durante settleMs (default800), min intervalo cooldownMs default2500; usa copias de buffers. Movimiento continuo no dispara spam; cambio estable significativo dispara una vez. Timestamp monotónico inyectable, tamaños nuevos reinician baseline. Defaults threshold .035. Tests secuencia sintética, copias, variación alfa, buffer inválido, primera imagen, cambio estabilizado, cooldown, reset.
Exporta async function startCameraMonitor(options:{video:HTMLVideoElement,onFrame:(dataUrl:string)=>Promise<void>,onStatus:(message:string)=>void,deviceId?:string}):Promise<{stop():void}>. Sólo al llamarla pide getUserMedia video ideal640x480 audio false; video muted playsInline. Canvas local 320x240, sample cada250ms. Usa ChangeGate y JPEG quality .75, await onFrame sin concurrencia; no acumular capturas. Errores de permisos/cámara/onFrame visibles onStatus; detener tracks y timers con stop idempotente. Si hay fallo getUserMedia/play limpiar streams; si no Canvas document entorno lanza claro. No URLs externas ni llamadas de red internas. Rechazo onFrame no mata cámara pero reintento limitado por cooldown, sin carrera tras stop. Documento calibración real pendiente y cámaras RTSP aún fuera; este módulo permite webcam navegador para demo. No tocar public/app.js ni index.html, el arquitecto integra.

NO TOCAR: otros archivos, dependencias, contratos, secretos, expediente. No commit/push.
