# Pilar voz
Crea src/voice.ts, test/voice.test.ts, docs/VOICE.md. Exporta function createVoice(options?:{env?:Record<string,string|undefined>;fetcher?:typeof fetch}):{status():{configured:boolean;provider:'elevenlabs';voiceId?:string;language:'es';state:'unconfigured'|'untested'|'ready'|'error'};synthesize(text:string,signal?:AbortSignal):Promise<Uint8Array>}.
ElevenLabs API oficial POST https://api.elevenlabs.io/v1/text-to-speech/{voiceId}/stream con header xi-api-key y content-type application/json. body text, model_id eleven_flash_v2_5, language_code es, voice_settings stability .45 similarity_boost .75. Voz env ELEVENLABS_VOICE_ID requerida (arquitecto seleccionó voz mexicana disponible, no hardcode); key requerida. Timeout 15s combinada con AbortSignal.any señal del caller para barge-in. Texto trim no vacío <=1600. Leer audio de respuesta con límite total 5MB y rechazar content-type no audio; no devolver JSON como MP3. status ready sólo tras audio válido no vacío. Nunca loguear key ni upstream error body. Sin persistir audio. Test body exacto, URL codificación voice id validar alfanumérico, falta configuración, cancelación, HTTP error, contenido no audio, límite tamaño y success. Documento distingue generación NVIDIA del texto de síntesis ElevenLabs y barge-in del navegador. No interfaces web aquí.

NO TOCAR: otros archivos, dependencias, contratos, secretos, expediente. No commit/push.
