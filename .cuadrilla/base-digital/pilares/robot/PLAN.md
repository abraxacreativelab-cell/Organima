# OBRA: robot
## Resultado observable
Implementa src/robot.ts, test/robot.test.ts y docs/ROBOT-CONTRACT.md. Exporta function createRobot(options:{mode:Mode; now?:()=>number}):RobotPort.
Simulator only: describe id robot parentId organism name Robot Cell, capability push_object, status ready en simulation y offline en live. submit en live falla explícitamente hardware no conectado. En simulation validar Goal completo, id y cellId robot, object red_ball target paper relation ON, deadline válido futuro y como máximo 120 segundos, mode simulation. Repetir mismo goal id y parámetros retorna estado sin reiniciar; mismo id distinto payload rechaza; segundo goal mientras uno activo rechaza. Máquina accepted -> running en primer tick, awaiting_verification después de mínimo 3 segundos acumulados desde aceptación; no produce relaciones ni prueba éxito propia. tick vencido -> failed salvo terminales. cancel terminaliza accepted/running/awaiting y es idempotente. verify sólo desde awaiting, exige fuente vision_global, relation source igual, confidence >=.85, subject/obj/predicate coincidentes y observedAt >= entrada awaiting_verification y <= ahora, no futuro. Resultado verified únicamente con evidencia válida; evidencia mala no cambia estado. status clones profundos. Límites de estado/id historial acotados documentados. Tests reloj falso de tiempo, deadline, duplicate/replay, cancel, wrong source, stale/future evidence y modo live. Documento protocolo futuro MCU, heartbeat/watchdog/override local y orden de pruebas humanas, sin inventar pines ni voltajes.

## NO TOCAR
Sólo los archivos nombrados arriba. No editar src/contracts.ts, package*, CLAUDE.md ni expediente. No git commit/push.
