## DEFECTOS
- `docs/COGNITION.md:66` — Afirma «`unconfigured`: falta secreto, modelo, o el modelo de chat/razonamiento no empieza con `nvidia/`». Es falso: medido en P1, con `NEBIUS_REASONING_MODEL=openbmb/MiniCPM-V-4_5` el estado devuelto es `configured: true, state: "untested"`, nunca `unconfigured` ni `error`, mientras `decide` y `reply` fallan en el 100% de los casos. La doc y el código se contradicen sobre el punto exacto que el módulo debe garantizar («estado honesto de proveedores», «unknown no implica conectado»). — Arreglo: hacer que `statuses()` consulte también `reasoningConfig()` para la fila `chat` y reporte `unconfigured` con la razón cuando el modelo de razonamiento efectivo sea inválido; si el arquitecto prefiere el otro camino, corregir la frase de la doc y decir explícitamente que la validación del razonamiento no se refleja en `statuses`.
- `src/cognition.ts:602` — `statuses()` sólo mira `providerConfig(key)`; el modelo de razonamiento introducido en esta obra no entra en ninguna de las tres filas, así que una mala configuración de `decide` es invisible para el operador y el estado nunca degrada. — Arreglo: mismo cambio de arriba, incorporando `reasoningConfig()` al cálculo de la fila `chat` (o exponiendo su `model` en el `detail`).
- `test/cognition.test.ts:548` — `assert.equal(cognition.statuses()[0].configured, true, 'el chat de conversación sigue configurado')` fija como correcto el comportamiento que la doc declara incorrecto, así que la regresión nunca se detectaría. — Arreglo: alinear la aserción con la decisión que se tome en los dos puntos anteriores.
- `test/cognition.test.ts:604` — `assert.ok(VISION_ENTITIES.includes('red_ball'))` dentro del bucle sobre `result.sources` es una aserción constantemente verdadera y sin relación con `research`. No debilita el resto del caso, pero es ruido que finge cobertura. — Arreglo: borrar la línea.

## RIESGOS
- Reintento por truncado sin cambiar condiciones: medido en P2, el segundo intento envía un cuerpo **idéntico** (`max_tokens: 600` en ambos, `JSON.stringify` iguales). Si Lightning trunca por presupuesto de tokens, el reintento truncará igual y sólo gasta una llamada de pago extra. Pasa los gates y cumple «reintento limitado» del plan, pero no aporta.
- `chatCompletionSchema` exige `message.content` string: un razonador de Nebius que devuelva `content: null` con `reasoning_content` falla con `invalid_response` (medido en P4). El fallo es explícito y no silencioso —correcto— pero el arquitecto debe confirmar con una llamada real que el modelo elegido sí llena `content` con `enable_thinking:false`.
- Documentación del repo desalineada, fuera de los archivos asignados (el constructor lo señaló y hace bien en no tocarlo): `docs/provider-contracts.md` sigue documentando Jev y proponiendo `nvidia/Nemotron-3-Nano-Omni` como visión —el modelo que da 404— mientras `docs/COGNITION.md:14` lo cita como «contrato verificado»; `.env.example` no lista `NEBIUS_REASONING_MODEL` y sí conserva `TYPESAFE_API_KEY`; `CLAUDE.md`, `README.md` y `docs/ARCHITECTURE.md` siguen diciendo «Jev obligatorio». Es deuda del arquitecto, no defecto del constructor, pero hoy el repo se contradice a sí mismo.
- Cero llamadas reales en esta obra: todo lo verificado aquí usa `fetcher` inyectado, como manda el contexto. Nada de lo que firmo prueba que Nebius o Tavily respondan; eso sigue siendo evidencia que le toca producir al arquitecto.
- `src/cognition.ts:621` tiene una rama muerta (`runtime[key].state === 'simulation'` nunca se asigna). Inofensivo.
