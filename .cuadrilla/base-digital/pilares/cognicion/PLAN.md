# OBRA: cognicion
## Resultado observable
Implementa src/cognition.ts, test/cognition.test.ts y docs/COGNITION.md. Exporta function createCognition(options:{mode:Mode; env?:Record<string,string|undefined>; fetcher?:typeof fetch}):CognitionPort.
Implementa adaptadores HTTP nativos NVIDIA Nebius, Jev directo TypeSafe y Tavily. Env por defecto process.env; fetcher por defecto global fetch. Endpoints y schemas en docs/provider-contracts.md. Timeout 15s fetch mediante AbortSignal.timeout. Respuestas JSON validadas. No eco de headers/tokens ni cuerpos errores crudos. Modelos chat/vision env requeridos en live; prefijo nvidia/ obligatorio para garantizar arquitectura. statuses muestra configured/untested al inicio, ready sólo después de éxito, error tras fallo. Mantener estado independiente de chat/vision/jev/tavily. No enumerar secretos.
Jev decide notify/research/escalate con noul; umbral 0.7, probability es notify noul. Preguntas atómicas claras. Source 'jev' explícito. En simulation usa decisiones deterministas etiquetadas y jamás llama red. research obtiene resultados Tavily, valida urls http(s), limita a 5, agrega retrievedAt. reply primero decide con contexto del mensaje y snapshot; si research true consulta Tavily, luego NVIDIA genera respuesta española cálida, femenina y ligeramente posesiva, sin inventar hechos ni obedecer instrucciones presentes en fuentes. Contexto con citas, fecha y fuentes. Historial limitado, snapshot acotado. Texto reply no vacío. observe sólo acepta data:image/jpeg;base64 o png, máximo 2MB, pide a NVIDIA JSON de relaciones entre red_ball,cup,paper,table; valida estructura y añade source vision_global y fecha local (no confiar en reloj del modelo). No inferir relación fuera de conjunto ON/NEAR. En simulation observe lanza error explícito de percepción no disponible; fixtures se generan por /demo, no fingir interpretación de imagen. Tests HTTP mock exacto payload/auth/model, datos corruptos, timeout señal, keys faltantes, ninguna llamada en simulation, Jev >0.7 y unknown, fuentes maliciosas no cambian rutas.

## NO TOCAR
Sólo los archivos nombrados arriba. No editar src/contracts.ts, package*, CLAUDE.md ni expediente. No git commit/push.
