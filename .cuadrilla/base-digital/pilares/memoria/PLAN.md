# OBRA: memoria
## Resultado observable
Implementa src/memory.ts y test/memory.test.ts. Exporta async function createMemory(directory:string):Promise<MemoryPort>.
Un escritor serializado JSONL en directory/events.jsonl (durable antes de actualizar proyección); createMemory reconstruye al reiniciar. Sólo aceptar OrganimaEvent válido: ids/cell/type no vacíos, fecha ISO válida no excesivamente futura, mode explícito, payload JSON. append retorna false si id duplicado, true si nuevo. Snapshot clones profundos, eventos últimos 100, grafo mantiene última relación por subject+predicate. Tipo observation payload={relations:Relation[]} se valida estrictamente, observedAt y source no vacíos, confianza 0..1. No mezclar relaciones live y simulation en el mismo store: fijar modo a primera escritura y rechazar otro modo. Observaciones viejas no pisan actuales. Evento oculto puede tener object unknown. Campos no observación no alteran relaciones. query busca términos sin distinguir mayúsculas en relaciones y eventos, retorno clonado. Contextos por cellId privados, acotados a últimos 20 y clonados; no persisten conversaciones privadas a knowledge. Rechazar path vacío; no escribir fuera de directory. Journal corrupto debe fallar explícitamente (no truncar ni perder datos en silencio). Implementar tests de reinicio, duplicación concurrente, orden, mutation leakage, aislamiento contexto, JSON inválido, mezcla modos. No dependas de otros módulos.

## NO TOCAR
Sólo los archivos nombrados arriba. No editar src/contracts.ts, package*, CLAUDE.md ni expediente. No git commit/push.
